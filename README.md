# QuickMoves

Implemented commands:

- Move cursor multiple lines relative to the current position (similar to `*j` and `*k` commands in vim)
- Change the headline level of all headlines in a selection
